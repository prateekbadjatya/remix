import React from 'react'

const FigmaImage = () => {
  return (
    <div>FigmaImage</div>
  )
}

export default FigmaImage



// You can opt out of nesting with a trailing underscore on the parent segment.

// So if you have a trailing underscore here.

// Now you can see that because I added the trailing underscore at this parent route This parent route's

// layout is not shared with the immediate children.
